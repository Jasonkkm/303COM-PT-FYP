<?php

// Check if the form is submitted
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    // Retrieve the form data
    $helperId = $_POST['helper_id'];
    $name = $_POST['name'];
    $age = $_POST['age'];
    $skills = $_POST['skills'];
    $experience = $_POST['experience'];
    $peopleToService = $_POST['people_to_service'];
    $roomType = $_POST['room_type'];
    $takeCareBaby = issetI apologize, but it seems that the content you provided for the `edit_helper_profile.php` and `update_helper_profile.php` is missing. Could you please provide the complete code for both files so that I can assist you further?